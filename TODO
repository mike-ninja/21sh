handle > without target
< segfaults
Error managment for redir, input redri` open fails if no existing file
Root leak with redir
Root leak with any command
if redir is before command, does not work
do >&- and reset fd functio
